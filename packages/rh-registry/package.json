{
  "name": "@vaultmcp/rh-registry",
  "version": "0.0.1",
  "private": true,
  "description": "MCP-server safety registry for the Robinhood Chain ecosystem: a directory of which trading MCP servers have been scanned by Vault and their safety status.",
  "type": "module",
  "scripts": {
    "serve": "tsx src/server.ts",
    "mcp": "tsx src/mcp-server.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
