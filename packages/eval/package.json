{
  "name": "@vaultmcp/eval",
  "version": "0.0.1",
  "description": "Vault Red Team — eval harness comparing Vault against Lakera / OpenAI Moderation / raw Anthropic judge",
  "private": true,
  "type": "module",
  "bin": {
    "vault-eval": "./dist/index.js"
  },
  "scripts": {
    "build": "tsup",
    "dev": "tsx src/index.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "score": "tsx src/index.ts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.0",
    "@vaultmcp/mcp-proxy": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsup": "^8.3.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
