{
  "name": "vault",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "beta:status": "node scripts/beta-status.mjs"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["sharp", "esbuild", "protobufjs", "better-sqlite3"]
  }
}
